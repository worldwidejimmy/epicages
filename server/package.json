{
  "name": "epic-ages-server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5",
    "tsconfig-paths": "^4.2.0",
    "tsc-alias": "^1.8.8"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only -r tsconfig-paths/register src/index.ts",
    "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "start": "node dist/index.js"
  }
}